import { Typography } from 'antd';

const { Title } = Typography;

const Rules = () => {
  return (
    <div>
      <Title>Forwarding Rules</Title>
      <p className="text-gray-400">Manage and test your forwarding rules here. You'll be able to create, edit, delete, and test rules.</p>
    </div>
  );
};

export default Rules;